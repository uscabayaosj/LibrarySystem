__pycache__/
instance/
*.pyc
*.pyo
.env
*.db
.DS_Store

# Per-deployment org branding uploads (logo). Not source -- each install's
# own logo shouldn't be committed or overwrite another install's via git.
static/uploads/
.vercel
.env*
