# Runtime dependencies, pinned to the versions in poetry.lock.
#
# pyproject.toml/poetry.lock remain the source of truth for local development.
# This file exists because most hosts (Render, Railway, Fly, Heroku) build with
# `pip install -r requirements.txt` out of the box. Regenerate after changing
# dependencies:  poetry export -f requirements.txt --without-hashes
Flask==3.0.3
Flask-Login==0.6.3
Flask-Migrate==4.1.0
Flask-SQLAlchemy==3.1.1
Flask-WTF==1.3.0
SQLAlchemy==2.0.34
alembic==1.18.5
gunicorn==26.0.0
pillow==12.3.0
psycopg2-binary==2.9.9
pywebpush==2.5.0
